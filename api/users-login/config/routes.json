{
  "routes": [
    {
      "method": "GET",
      "path": "/users-logins",
      "handler": "users-login.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/users-logins/count",
      "handler": "users-login.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/users-logins/:id",
      "handler": "users-login.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/users-logins",
      "handler": "users-login.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/users-logins/:id",
      "handler": "users-login.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/users-logins/:id",
      "handler": "users-login.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
